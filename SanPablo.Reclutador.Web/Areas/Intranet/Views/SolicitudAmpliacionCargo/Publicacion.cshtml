@model SanPablo.Reclutador.Web.Areas.Intranet.Models.SolicitudRempCargoViewModel
@using SanPablo.Reclutador.Entity
@using System.Collections.Generic

@{
    ViewBag.Title = "Publicacion";
}
@section Head{
}

@section Scripts{

<script src="@Url.Content("~/Scripts/jquery.validate.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.js")" type="text/javascript"></script>

<script type="text/javascript">
        
    $(document).ready(function () {

        var dateToday = new Date();

        var dates = $('#SolReqPersonal_FecPublicacion,#SolReqPersonal_FecExpiracacion').datepicker({
            showOn: "button",
            buttonImage: '@Url.Content("~/Content/images/calendario.png")',
            buttonImageOnly: true,
            //defaultDate: "+1w",
            changeMonth: true,
            numberOfMonths: 1,
            minDate: dateToday,
            onSelect: function (selectedDate) {
                var option = this.id == "from" ? "minDate" : "maxDate",
                    instance = $(this).data("datepicker");
                //    date = $.datepicker.parseDate(instance.settings.dateFormat || $.datepicker._defaults.dateFormat, selectedDate, instance.settings);
                //dates.not(this).datepicker("option", option, date);
            }
        });


        $('#SolReqPersonal_FecPublicacion,#SolReqPersonal_FecExpiracacion').attr("readonly", "readonly");

        
        $('#btnPublicarSolReq').click(function () {
            if (validaPublicacion)
            {
                $.ajax({
                data: $("#frmPublicaSolReemplazoCargo").serialize(),
                type: "POST",
                url: '@Url.Action("PublicaSolReqPersonal", "SolicitudAmpliacionCargo", new { area="Intranet"})',
                traditional: true,
                    success: function (data) {
                        if (data.Resultado)
                        {
                            // MostrarMensaje(data.Mensaje, "Sistema de Reclutamiento Y Seleccion de Personal");
                          information("divMensaje", "spnMensaje", data.Mensaje, "Sistema de Reclutamiento Y Seleccion de Personal");

                        }
                    }
                });
            }
       });


        $('#btnAnteriorPublica').click(function () {
            var selr = $("#SolReqPersonal_IdeSolReqPersonal").val();

            window.location = '@Url.Action("Edit", "SolicitudAmpliacionCargo", new { id = "", area = "Intranet"})' + '/' + selr;

        });
        


        jQuery("#grdListConocimientos").jqGrid({
            datatype: function (postdata) {
                var migrilla = new Object();
                migrilla.page = postdata.page;
                migrilla.rows = postdata.rows;
                migrilla.sidx = postdata.sidx;
                migrilla.sord = postdata.sord;
                migrilla._search = postdata.isSearch;
                migrilla.filters = postdata.filters;
                migrilla.Rules = [
                       { field: 'idSol', data: $("#SolReqPersonal_IdeSolReqPersonal").val() },

                ];

                if (migrilla._search == true) {
                    migrilla.searchField = postdata.searchField;
                    migrilla.searchOper = postdata.searchOper;
                    migrilla.searchString = postdata.searchString;
                }

                var params = { grid: migrilla };

                $.ajax({
                    url: '@Url.Action("Conocimientos", "SolicitudAmpliacionCargo")',
                    type: 'post',
                    contentType: 'application/json; charset=utf-8',
                    data: JSON.stringify(params),
                    async: false,
                    success: function (data, st) {
                        if (st == 'success') {
                            var jq = $('#grdListConocimientos')[0];
                            jq.addJSONData(data);
                        }
                    },
                    error: function (e) {
                        console.log(e);
                    }
                });
            },
            colNames: ['Tipo de Conocimiento', 'Descripci&oacuten'],
            colModel: [
                    { name: 'TipoConocimiento', index: 'TipoConocimiento', align: 'left', editable: false, sortable: false },
                    { name: 'Descripcion', index: 'Descripcion', align: 'left', editable: false, sortable: false, width: 600 }
            ],

            pager: jQuery('#grdPagerEstudios'),
             rowNum: 10,
             //rowList: [10, 30, 50],
             autowidth: true,
             rules: true,
             sortname: 'IdeConocimientoGeneralRequerimiento',
             sortorder: 'desc',
             viewrecords: true,
             height: 180,
             width: 600,
             cellsubmit: 'clientArray',
             hidegrid: false,
             rownumbers: true,
             shrinkToFit: false,

             postData: {
             },
             loadComplete: function (data) {
             }
         });


        jQuery("#grdListEstudios").jqGrid({
            datatype: function (postdata) {
                var migrilla = new Object();


                migrilla.page = postdata.page;
                migrilla.rows = postdata.rows;
                migrilla.sidx = postdata.sidx;
                migrilla.sord = postdata.sord;
                migrilla._search = postdata.isSearch;
                migrilla.filters = postdata.filters;
                migrilla.Rules = [
                       { field: 'idSol', data: $("#SolReqPersonal_IdeSolReqPersonal").val() },

                ];

                if (migrilla._search == true) {
                    migrilla.searchField = postdata.searchField;
                    migrilla.searchOper = postdata.searchOper;
                    migrilla.searchString = postdata.searchString;
                }

                var params = { grid: migrilla };

                $.ajax({
                    url: '@Url.Action("Estudios", "SolicitudAmpliacionCargo")',
                    type: 'post',
                    contentType: 'application/json; charset=utf-8',
                    data: JSON.stringify(params),
                    async: false,
                    success: function (data, st) {
                        if (st == 'success') {
                            var jq = $('#grdListEstudios')[0];
                            jq.addJSONData(data);
                        }
                    },
                    error: function (e) {
                        console.log(e);
                    }
                });
            },
            colNames: ['Descripci&oacuten'],
            colModel: [
                    
                    { name: 'Descripcion', index: 'Descripcion', align: 'left', editable: false, sortable: false, width: 600 }
            ],

            pager: jQuery('#grdPagerConocimientos'),
            rowNum: 10,
            //rowList: [10, 30, 50],
            autowidth: true,
            rules: true,
            sortname: 'IdeNivelAcademicoRequerimiento',
            sortorder: 'desc',
            viewrecords: true,
            height: 180,
            width: 600,
            cellsubmit: 'clientArray',
            hidegrid: false,
            rownumbers: true,
            shrinkToFit: false,

            postData: {
            },
            loadComplete: function (data) {
            }
        });


        jQuery("#grdListCompetencias").jqGrid({
            datatype: function (postdata) {
                var migrilla = new Object();


                migrilla.page = postdata.page;
                migrilla.rows = postdata.rows;
                migrilla.sidx = postdata.sidx;
                migrilla.sord = postdata.sord;
                migrilla._search = postdata.isSearch;
                migrilla.filters = postdata.filters;
                migrilla.Rules = [
                       { field: 'idSol', data: $("#SolReqPersonal_IdeSolReqPersonal").val() },

                ];

                if (migrilla._search == true) {
                    migrilla.searchField = postdata.searchField;
                    migrilla.searchOper = postdata.searchOper;
                    migrilla.searchString = postdata.searchString;
                }

                var params = { grid: migrilla };

                $.ajax({
                    url: '@Url.Action("Competencias", "SolicitudAmpliacionCargo")',
                    type: 'post',
                    contentType: 'application/json; charset=utf-8',
                    data: JSON.stringify(params),
                    async: false,
                    success: function (data, st) {
                        if (st == 'success') {
                            var jq = $('#grdListCompetencias')[0];
                            jq.addJSONData(data);
                        }
                    },
                    error: function (e) {
                        console.log(e);
                    }
                });
            },
            colNames: ['Descripci&oacuten'],
            colModel: [
                    { name: 'Descripcion', index: 'Descripcion', align: 'left', editable: false, sortable: false, width: 600 }
            ],

            pager: jQuery('#grdPagerCompetencias'),
            rowNum: 10,
            //rowList: [10, 30, 50],
            autowidth: true,
            rules: true,
            sortname: 'IdeCompetenciaRequerimiento',
            sortorder: 'desc',
            viewrecords: true,
            height: 180,
            width: 600,
            cellsubmit: 'clientArray',
            hidegrid: false,
            rownumbers: true,
            shrinkToFit: false,

            postData: {
            },
            loadComplete: function (data) {
            }
        });



        jQuery("#grdListExperiencia").jqGrid({
            datatype: function (postdata) {
                var migrilla = new Object();


                migrilla.page = postdata.page;
                migrilla.rows = postdata.rows;
                migrilla.sidx = postdata.sidx;
                migrilla.sord = postdata.sord;
                migrilla._search = postdata.isSearch;
                migrilla.filters = postdata.filters;
                migrilla.Rules = [
                       { field: 'idSol', data: $("#SolReqPersonal_IdeSolReqPersonal").val() },

                ];

                if (migrilla._search == true) {
                    migrilla.searchField = postdata.searchField;
                    migrilla.searchOper = postdata.searchOper;
                    migrilla.searchString = postdata.searchString;
                }

                var params = { grid: migrilla };

                $.ajax({
                    url: '@Url.Action("Experiencia", "SolicitudAmpliacionCargo")',
                    type: 'post',
                    contentType: 'application/json; charset=utf-8',
                    data: JSON.stringify(params),
                    async: false,
                    success: function (data, st) {
                        if (st == 'success') {
                            var jq = $('#grdListExperiencia')[0];
                            jq.addJSONData(data);
                        }
                    },
                    error: function (e) {
                        console.log(e);
                    }
                });
            },
            colNames: ['Descripci&oacuten', 'Tiempo de Experiencia'],
            colModel: [
                    { name: 'Descripcion', index: 'Descripcion', align: 'left', editable: false, sortable: false, width: 400 },
                    { name: 'TiempoExperiencia', index: 'TiempoExperiencia', align: 'left', editable: false, sortable: false, width: 200 },
            ],

              pager: jQuery('#grdPagerExperiencia'),
              rowNum: 10,
              //rowList: [10, 30, 50],
              autowidth: true,
              rules: true,
              sortname: 'IdeExperienciaRequerimiento',
              sortorder: 'desc',
              viewrecords: true,
              height: 180,
              width: 600,
              cellsubmit: 'clientArray',
              hidegrid: false,
              rownumbers: true,
              shrinkToFit: false,

              postData: {
              },
              loadComplete: function (data) {
              }
          });

 

        jQuery("#grdListOfrecemos").jqGrid({
            datatype: function (postdata) {
                var migrilla = new Object();


                migrilla.page = postdata.page;
                migrilla.rows = postdata.rows;
                migrilla.sidx = postdata.sidx;
                migrilla.sord = postdata.sord;
                migrilla._search = postdata.isSearch;
                migrilla.filters = postdata.filters;
                migrilla.Rules = [
                       { field: 'idSol', data: $("#SolReqPersonal_IdeSolReqPersonal").val() },

                ];

                if (migrilla._search == true) {
                    migrilla.searchField = postdata.searchField;
                    migrilla.searchOper = postdata.searchOper;
                    migrilla.searchString = postdata.searchString;
                }

                var params = { grid: migrilla };

                $.ajax({
                    url: '@Url.Action("Ofrecemos", "SolicitudAmpliacionCargo")',
                    type: 'post',
                    contentType: 'application/json; charset=utf-8',
                    data: JSON.stringify(params),
                    async: false,
                    success: function (data, st) {
                        if (st == 'success') {
                            var jq = $('#grdListOfrecemos')[0];
                            jq.addJSONData(data);
                        }
                    },
                    error: function (e) {
                        console.log(e);
                    }
                });
            },
            colNames: ['Descripci&oacuten'],
            colModel: [
                    { name: 'Descripcion', index: 'Descripcion', align: 'left', editable: false, sortable: false, width: 600 },
            ],

             pager: jQuery('#grdPagerOfrecemos'),
             rowNum: 10,
             //rowList: [10, 30, 50],
             autowidth: true,
             rules: true,
             sortname: 'IdeOfrecemosRequerimiento',
             sortorder: 'desc',
             viewrecords: true,
             height: 180,
             width: 600,
             cellsubmit: 'clientArray',
             hidegrid: false,
             rownumbers: true,
             shrinkToFit: false,

             postData: {
             },
             loadComplete: function (data) {
             }
         });

    });

    function validaPublicacion() {

        var fecInicial = $("#SolReqPersonal_FecPublicacion").val();
        var fecFinal = $("#SolReqPersonal_FecExpiracacion").val();

        var firstValue = fecInicial.split('-');
        var secondValue = fecFinal.split('-');

        if (firstValue == 0 && firstValue=="") {
            
            MostrarMensaje('Ingrese fecha de publicación', "Sistema de Reclutamiento Y Seleccion de Personal");
            return false;
        }
        if (secondValue == 0 && secondValue=="") {
            
            MostrarMensaje('Ingrese fecha de expiración', "Sistema de Reclutamiento Y Seleccion de Personal");
            return false;
        }

        var firstDate = new Date();
        firstDate.setFullYear(firstValue[0], (firstValue[1] - 1), firstValue[2]);

        var secondDate = new Date();
        secondDate.setFullYear(secondValue[0], (secondValue[1] - 1), secondValue[2]);

        if (firstDate > secondDate) {
            MostrarMensaje('Fecha de publicación no puede ser mayor a la fecha de experización', "Sistema de Reclutamiento Y Seleccion de Personal");
            return false;
        }
        return true;


    }


    function MostrarMensaje(mensaje, titulo) {
        Funciones.Alert("divMensaje", "spnMensaje", mensaje, titulo);
    }


    function information(selectorDiv, selectorSpan, msg, title) {
        $('#' + selectorSpan).html(msg);
        var opt = {
            title: title,
            autoOpen: false,
            modal: true,
            buttons: {
                "Aceptar": function () {
                    $(this).dialog("close");
                    RedireccionaBus();
                }
            }
        };
        $('#' + selectorDiv).dialog(opt).dialog('open');

    }

    function RedireccionaBus() {
        window.location = '@Url.Action("Index", "SolicitudAmpliacionCargo", new { area = "Intranet"})';
    }


</script>

}     
        <div class="span12">
        <div class="widget-box">
            <div class="widget-title">
                <h5>@Model.SolReqPersonal.nombreCargo - @Model.SolReqPersonal.DesCargo</h5>
            </div>
@using (Html.BeginForm("Publicacion", "SolicitudAmpliacionCargo", FormMethod.Post, new { enctype = "multipart/form-data", id = "frmPublicaSolReemplazoCargo"}))
{
    @Html.ValidationSummary(true)

    
     @Html.TextBoxFor(model => model.SolReqPersonal.IdeSolReqPersonal, new { style = "display: none;" })
    
                <fieldset>
                    <legend></legend>
                    <table class="formulario-busqueda">

                    <tr class="formulario-fila">
                        <td class="span5">
                            <label class="control-label" for="inputSuccess">Area</label>
                            <div class="controles">                         
                              @Html.TextBoxFor(model => model.SolReqPersonal.Area_des, new { @class = "textoFormulario" ,disabled ="disabled" }) 
                            </div>
                        </td>
                    </tr>
                      
                     <tr class="formulario-fila">
                        <td class="span5">
                            <label class="control-label" for="inputSuccess">Tipo</label>
                            <div class="controles">                         
                              @Html.TextBoxFor(model => model.SolReqPersonal.TipPuestoDes, new { @class = "textoFormulario" ,disabled ="disabled" }) 
                            </div>
                        </td>
                    </tr>

                    <tr class="formulario-fila">
                        <td class="span5">
                            <label class="control-label" for="inputSuccess">Vacantes</label>
                            <div class="controles">                         
                              @Html.TextBoxFor(model => model.SolReqPersonal.NumVacantes, new { @class = "textoFormulario" ,disabled ="disabled" }) 
                            </div>
                        </td>
                    </tr>

                    <tr class="formulario-fila">
                        <td class="span5">
                            <label class="control-label" for="selectError">Rango salarial</label>
                            <div class="controles">
                                @Html.DropDownListFor(model => model.SolReqPersonal.TipoRangoSalario, new SelectList(Model.listaRangoSalarial, "Valor", "Descripcion", Model.SolReqPersonal.TipoRangoSalario), new { @disabled = "disabled"})
                                
                                @*<label class="checkbox" style="display:inline-table"><input type="checkbox" id="IndVerSalario" name="IndVerSalario" class="chkclass" value="@Model.SolReqPersonal.IndicadorSalario" /> No Publicar</label>*@
                               
                            </div>
                        </td>
                        
                    </tr>
                    <tr class="formulario-fila">
                        <td class="span5">
                            <label class="control-label" for="selectError">Publicar</label>
                            <div class="controles">
                                @if (TipoSolicitud.ConsultaRequerimientos.Equals(Model.Pagina))
                                {
                                    @Html.CheckBoxFor(model => model.verSalario, new { @class="checkbox", disabled = "disabled" }) 
                                }
                                else
                                {
                                    @Html.CheckBoxFor(model => model.verSalario, new { @class="checkbox" })
                                }
                            </div>
                        </td>
                        
                    </tr>
                         
                    <tr class="formulario-fila">
                        <td class="span5">
                            <label class="control-label" for="selectError">Fecha de publicaci&oacute;n</label>
                            <div class="controles">
                               @Html.TextBoxFor(model => model.SolReqPersonal.FecPublicacion, new { @class = "textoFormulario" ,  maxlength="15", placeholder=" Inicial"})      
                            </div>
                        </td>
                    </tr>

                      <tr class="formulario-fila">
                        <td class="span5">
                            <label class="control-label" for="selectError">Fecha de expiraci&oacute;n</label>
                            <div class="controles">
                               @Html.TextBoxFor(model => model.SolReqPersonal.FecExpiracacion, new { @class = "textoFormulario" ,  maxlength="15", placeholder=" Inicial"})      
                            </div>
                        </td>
                    </tr>


                    </table>

                   <table style="margin-left:50px;" >
                   <tr><td>&nbsp;</td></tr>
                   <tr class="formulario-fila">
                        <td class="span5">
                        <label class="control-label" for="input01">Estudios</label>
                         <div class="controls">
                            <div style="width: 100%">                                        
                                <div>
                                    <table id="grdListEstudios">
                                    </table>
                                    <div id="grdPagerEstudios">
                                    </div>
                                </div>                                         
                            </div>
                         </div>
                        </td>
                    </tr>
                        <tr><td>&nbsp;</td></tr>
                    <tr class="formulario-fila">
                        <td class="span5">
                        <label class="control-label" for="input01">Conocimientos</label>
                         <div class="controls">
                            <div style="width: 100%">                                        
                                <div>
                                    <table id="grdListConocimientos">
                                    </table>
                                    <div id="grdPagerConocimientos">
                                    </div>
                                </div>                                         
                            </div>
                         </div>
                        </td>
                    </tr>
                    <tr><td>&nbsp;</td></tr>
                    <tr class="formulario-fila">
                        <td class="span5">
                        <label class="control-label" for="input01">Experiencia</label>
                         <div class="controls">
                            <div style="width: 100%">                                        
                                <div>
                                    <table id="grdListExperiencia">
                                    </table>
                                    <div id="grdPagerExperiencia">
                                    </div>
                                </div>                                         
                            </div>
                         </div>
                       </td>
                    </tr>
                        <tr><td>&nbsp;</td></tr>
                   <tr class="formulario-fila">
                        <td class="span5">
                        <label class="control-label" for="input01">Funciones</label>
                         <div class="controls">
                            @Html.TextAreaFor(model=>model.SolReqPersonal.FuncionesCargo, new {@disabled = "disabled", @style = "width:600px; height:100px"})
                         </div>
                       </td>
                    </tr>
                        <tr><td>&nbsp;</td></tr>
                    <tr class="formulario-fila">
                        <td class="span5">
                        <label class="control-label" for="input01">Competencias</label>
                         <div class="controls">
                            <div style="width: 100%">                                        
                                <div>
                                    <table id="grdListCompetencias">
                                    </table>
                                    <div id="grdPagerCompetencias">
                                    </div>
                                </div>                                         
                            </div>
                         </div>
                       </td>
                    </tr>
                        <tr><td>&nbsp;</td></tr>
                    <tr class="formulario-fila">
                        <td class="span5">
                        <label class="control-label" for="input01">Ofrecemos</label>
                         <div class="controls">
                            <div style="width: 100%">                                        
                                <div>
                                    <table id="grdListOfrecemos">
                                    </table>
                                    <div id="grdPagerOfrecemos">
                                    </div>
                                </div>                                         
                            </div>
                         </div>
                        </td>
                    </tr>
                        <tr><td>&nbsp;</td></tr>
                    <tr class="formulario-fila">
                        <td class="span5">
                        <label class="control-label" for="input01">Observaciones</label>
                         <div class="controls">
                             @if(TipoSolicitud.ConsultaRequerimientos.Equals(Model.Pagina))
                             {
                                @Html.TextAreaFor(model => model.SolReqPersonal.ObservacionPublica, new { @style = "width:600px;height:100px", disabled = "disabled"})
                             }
                             else
                             {
                                @Html.TextAreaFor(model=>model.SolReqPersonal.ObservacionPublica, new { @style = "width:600px;height:100px", maxlength = "255", disabled = "disabled", onkeyup="aMays(event, this)"  })     
                             }
                         </div>
                        </td>
                    </tr>   
                                                
                  
                </table>                       
                              

                </fieldset>
                <div class="form-actions"> 
                    @if (TipoSolicitud.ConsultaRequerimientos.Equals(Model.Pagina))
                    {
                        <a class="btn"  href="@Url.Action("Index", "SolicitudConsulta")"><i class="icon-arrow-left icon-cog"></i> Anterior</a>     
                    }
                    else
                    {
                        <a class="btn"  id="btnAnteriorPublica"><i class="icon-arrow-left icon-cog"></i> Anterior</a>    
                    }
                    @if (Visualicion.SI.Equals(Model.btnPublicar))
                    {
                        <a class="btn btn-info" id="btnPublicarSolReq"><i class="icon-circle-arrow-up icon-white"></i> Publicar</a>
                    }
                    @if (Visualicion.SI.Equals(Model.btnActualizar))
                    {
                        <a class="btn btn-info" id="btnActualizar"><i class="icon-circle-arrow-up icon-white"></i> Actualizar</a>    
                    }
                         
                </div>
}
                  
            </div>
        </div>



